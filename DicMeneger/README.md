# RoleBot (ежедневные задачи + медиа)

## Установка
1. Переименуйте `.env.example` в `.env` и заполните значения.
2. Установите зависимости:
   ```bash
   npm i
   ```
3. Запуск:
   ```bash
   npm start
   ```

## Что внутри
- Ежедневное расписание (каждый день), время в `.env` (`START_TIME`, `END_TIME`).
- Назначение задач списком/одной задачей.
- Режим **«📎 Фото/Видео/Документ»** — пересылает вложения исполнителю.
- Экспорт XLSX/PDF.
- Данные в `data/db.json`.

## Команды
- `/myid` — показать Telegram ID.
- Кнопки: `🆕 Назначить`, `📨 Входящие`, `📊 Статус сегодня` (админ), `📤 Экспорт`.
- `/admins`, `/addadmin <id>`, `/rmadmin <id>`.

# AI-HealthBot (универсальный)
- Профили: 🧠 Общий, 🤰 Беременность, 👨 Мужчины, 👩‍🍼 Мамы и дети
- RU/UZ локализация
- Текст, фото/PDF, **голос с распознаванием (Whisper)**
- Docker/PM2 готово

## Быстрый старт
1) `.env.example` → `.env` — вставьте `BOT_TOKEN`, при использовании OpenAI добавьте `OPENAI_API_KEY`.
2) Локально: `npm i && npm start`
3) Docker: `docker compose up -d --build`
4) PM2: `pm2 start pm2.config.cjs && pm2 save`

## Голос
- Telegram: OGG/OPUS → **ffmpeg** конвертирует в MP3 (16kHz, mono) → **OpenAI Whisper (whisper-1)** → текст → LLM.
- В Docker-образ добавлен `ffmpeg`.
